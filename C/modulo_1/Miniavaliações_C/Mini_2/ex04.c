#include <stdio.h>

int main(){
    int valor, x, k, y;
    int somaDosNumeros, primeiroNumero, segundoNumero;
    int umInteiro;

    scanf("%d", &valor);

    printf("O produto de %d e %d e %d\n", x, k, y);

    somaDosNumeros = primeiroNumero + segundoNumero;

    /* Programa para determinar o maior de tres inteiros*/

    scanf("%d", &umInteiro);

    printf("O resto de %d dividido por %d e \n", x, y);

    printf("A soma e %d %d\n", x+y);

    printf("O  valor fornecido e: %d\n", valor);

    return 0;
}